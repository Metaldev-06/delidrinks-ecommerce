import { PayConditionPipe } from './pay-condition.pipe';

describe('PayConditionPipe', () => {
  it('create an instance', () => {
    const pipe = new PayConditionPipe();
    expect(pipe).toBeTruthy();
  });
});
